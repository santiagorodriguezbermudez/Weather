const Weather = ((city, country, weather, wind, main, sunrise, sunset, timezone) => (
  {
    city,
    country,
    weather,
    wind,
    main,
    sunrise,
    sunset,
    timezone,
  }));
export default Weather;