const Weather = ((city, country, expected, wind, humidity, temp, feelt, maxt, lowt, icon) => (
  {
    city,
    country,
    expected,
    wind,
    humidity,
    temp,
    feelt,
    maxt,
    lowt,
    icon,
  }));
export default Weather;